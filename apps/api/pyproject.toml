[build-system]
requires = ["hatchling>=1.27,<2"]
build-backend = "hatchling.build"

[project]
name = "gaiafaac-api"
version = "0.1.0"
description = "GaiaFAAC Intelligence public API"
requires-python = ">=3.12"
dependencies = [
  "alembic>=1.17,<2",
  "fastapi>=0.116,<1",
  "openpyxl>=3.1,<4",
  "pydantic-settings>=2.10,<3",
  "psycopg[binary]>=3.2,<4",
  "sqlalchemy>=2.0,<3",
  "stripe>=14.4,<15",
  "uvicorn[standard]>=0.35,<1",
]

[project.scripts]
gaiafaac-db = "gaiafaac_api.cli:main"
gaiafaac-national = "gaiafaac_api.national_cli:main"
gaiafaac-lga = "gaiafaac_api.lga_cli:main"

[project.optional-dependencies]
dev = [
  "httpx>=0.28,<1",
  "pytest>=8.4,<9",
  "pytest-cov>=6.2,<7",
  "ruff>=0.12,<1",
]
extraction = [
  "pdfplumber>=0.11,<0.12",
]

[tool.hatch.build.targets.wheel]
packages = ["src/gaiafaac_api"]

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[tool.ruff.format]
exclude = ["src/gaiafaac_api/pipeline/national_evidence_hardened.py"]
